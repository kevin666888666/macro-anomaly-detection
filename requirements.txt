# Requirements for macro-anomaly-detection
#
# 安装:  pip install -r requirements.txt
#
# 说明: WindPy 不通过 pip 安装 —— 它随 Wind 金融终端一起提供。
#       如果只想跑分析部分(用仓库里已保存的 CSV),不需要 WindPy。

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.3

# WindPy — 仅在需要重新取数时使用,需 Wind 终端运行中
# WindPy
